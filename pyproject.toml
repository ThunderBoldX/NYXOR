[project]
name = "nyxor"
version = "0.5.0"
description = "NYXOR terminal drop automation interface"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "rich",
    "textual",
]

[project.scripts]
nyxor = "nyxor_app:main"
